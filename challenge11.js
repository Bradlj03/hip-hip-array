// Challenge 11
// Make a COPY of the `students` array *using .slice*, 
//starting at index 3 and ending at index 10 (INCLUSIVE), 
//then log out each value of the copy individually. 
//THEN log out each value of the original array. Notice that 
//the original array didn't get any values taken out of it, 
//because `slice` does NOT modify the original array.

let students = [
    'Tre',
    'Sonny',
    'Crystal',
    'Ilyas',
    'Greg',
    'Fernando',
    'Timothy',
    'Patrick',
    'Steve',
    'Jimothy',
    'Pat',
    'Arnold',
    'Andy'
  ];

let studentsCopy = []; //starting and 3 and incluing 10

for (let count = 0; count < studentsCopy.length; count++) {
    studentsCopy = studentsCopy[count];
    console.log(studentsCopy);
    
}







